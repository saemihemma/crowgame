{
  "_comment": "Open product decisions, one entry each. Everything here is a question about a five-year-old that no amount of code reading settles -- how much counting is too much, whether losing the jump key feels wrong, how long a death should hold -- so it ships as a toggle a grown-up can FEEL rather than a constant somebody guessed. Read through Config.flag(); overridden per-device in the grown-up panel (ui/parent_report.gd), and in a debug build also from ?flags=name:value.\n\nA flag lives here only while its decision is genuinely open. Once it is settled, DELETE the entry AND the losing branch in the same commit. A flag that outlives its decision is how a codebase acquires two of everything and a test matrix nobody runs -- and the deletion is the whole reason this file is allowed to exist at all. Nothing here is a config surface for players; there is no UI that lists them by name.",
  "math": {
    "_comment": "The three phase-1 selector changes, separable on purpose: retiring a domain and flooring representation both reduce how much counting a child meets, so landing them together would make it impossible to tell which one did the work. tools/sim_learner_journey.ts is the instrument -- run it after touching any of these.",
    "retire_exhausted_domains": true,
    "_retire_exhausted_domains": "When a child has finished a domain's whole ladder, stop offering it. OwlSelection picks the next subject by staleness x weight and has no notion of mastery, so a domain at its ceiling is not merely still eligible -- the longer since it was served, the MORE due it becomes. Measured on the journey sim: counting tops out at step 6 in every journey and is then served 184/1200 to a thriving child, 293 to a steady one and 530 to a struggling one, from a pool of only 125 problems. OFF restores that.",
    "representation_floor": true,
    "_representation_floor": "Never ask a child to count more than ten things one at a time once they can compose a ten. This is the rule that nothing in the codebase owned: the ladder is per-domain, concepts partition steps inside a domain, and the child is one person having one sequence -- so addition.teen_numbers could teach that thirteen is one ten and three, and the very next question could be thirteen ungrouped marks with the hint 'count each dot carefully'. Not a weight tweak: lowering counting's share halves how often the game contradicts itself, it does not stop it.",
    "group_tokens_in_fives": true,
    "_group_tokens_in_fives": "Whether the counting board splits its tokens five-and-five, making it a ten-frame rather than a line of ten. This was ALREADY implemented in count_row.gd before the flag existed -- ten per row with a centre gap -- so the flag exists to make the difference feelable rather than to add it: off gives the unbroken row a line of marks in a text label always was. The split is what lets a child read eight as 'five and three' instead of counting to eight, which is the same idea representation_floor protects, one layer down in the drawing."
  },
  "input": {
    "space_is_sprint": true,
    "_space_is_sprint": "Space stops being a jump key and becomes hold-to-accelerate; W and Up keep jumping. This also gates keyboard answering, because the two cannot be separated: ui/components/answer_button.gd sets FOCUS_NONE precisely BECAUSE space is jump -- arrow keys walked the focus ring across the four options and ui_accept (space and enter) committed whichever one it landed on, so the game answered its own first question. Take space off jump and the keyboard path is safe again. OFF restores both old behaviours together, never one without the other.",
    "sprint_pad_latches": false,
    "_sprint_pad_latches": "Whether the on-screen sprint pad stays down until tapped again. OFF: it is a plain button that is down while a thumb is on it, exactly like the Space key. This was ON first, on the reasoning that a running jump needs direction + sprint + jump held together and this layout has two thumbs -- which was wrong, and the Game Boy is why. A and B sit side by side there: one thumb either covers both or leaves B for the instant it takes to press A, and Mario carries his speed through that instant. This crow does too. Measured against a 6-tile gap: sprint held through the jump reaches 9.83 tiles, letting go and jumping at once reaches 6.55, letting go a quarter-second early reaches 6.00, and never sprinting reaches 5.94. So the roll clears the gap and a slow thumb is the thing that does not -- which is why the pad now sits NEXT TO JUMP rather than with the directions, and why sprintDecelPerSec (260) is the number to lower if it feels tight on a device: it is what decides how long the run survives a thumb leaving the button. ON is still here for a child who cannot manage the roll at all: one tap latches the run and the lit plate says it is on. The cost of ON is that a child who forgets is running through level 7, whose one-tile stones want walking."
  },
  "levels": {
    "wide_gap_pass": true,
    "_wide_gap_pass": "The revised geometry for levels 2-5. Measured before writing it: every jumpable gap in all eight levels is three tiles, against a jump that reaches 5.9 -- so there is no distance challenge anywhere, and the wide spacings in levels 1/3/4/5 are descents where gravity does the work. Levels 6-8 differ only in platform WIDTH (one-tile pads) and in chaining hops without a rest. OFF restores the shipped layouts so a level can be A/B'd by reloading rather than by reading a diff.",
    "sprint_gaps_from": 6,
    "_sprint_gaps_from": "The first level allowed to contain a gap that WANTS a wound-up run. Below it every gap is built inside walk reach -- 5.9 tiles measured, designed to 4 with margin -- because requiring a held run key to survive is too hardcore for a child still learning to jump. From this level up the budget rises to the sprint reach of 9.8 tiles, designed to 8. A number, not a boolean, because 'how early is too early' is the actual question and 6 is a guess: lower it to let one gap in level 5 want a run, raise it to give the key longer to become a habit. WANTS, not needs: sprint is a default capability and nothing teaches it, so the reachability guard now runs a second pass with the sprint budget taken away, and the 6-tile gaps in levels 6 and 8 each carry one foothold a row below the ground line -- out of the running leap's arc, so sprinting still crosses them in one clean line, and the slow way down-across-up is always there. Before those footholds existed a child who never found Space was stopped at column 17 of level 6 with the door and 37 coins on the far side. If sprint is ever taught, that second pass is the thing to relax.",
    "practice_arena_in_grid": false,
    "_practice_arena_in_grid": "Whether level_99, the Math Practice Arena, appears as a card in the child's level grid. It held order:1, which put a flat 200-tile run of nineteen owls in the first slot and left every other card sitting one position ahead of its own level number -- so the fourth card was level three. The ordering is fixed outright; this is only about whether the arena is in the grid at all. OFF loses the one place a child can drill without platforming, which is a real cost: if it stays off, free practice needs an entry point a grown-up can find."
  },
  "death": {
    "hold_ms": 1200,
    "_hold_ms": "How long the death beat holds before the level reloads. Was 800ms at zero lives and NOTHING on the path a child actually hits -- hurt_player() with lives left teleported them to the spawn with no beat at all. A number rather than a boolean because 'a death scene' is a duration decision: a five-year-old needs longer than an adult to understand what happened, and much less before it turns into punishment. Watch a child, not yourself; if it reads as the game hanging it is too long."
  }
}
